<div class="prose code-block-wrapper">
  <slot />
</div>

<style>
  .code-block-wrapper {
    padding: clamp(0.95rem, 1.4vw, 1.25rem);
    border-radius: 22px;
    border: 1px solid rgba(218, 205, 184, 0.92);
    background:
      radial-gradient(circle at 95% 4%, rgba(31, 120, 117, 0.14), transparent 26%),
      linear-gradient(160deg, rgba(255, 255, 255, 0.96), rgba(246, 238, 223, 0.92));
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.48),
      0 20px 46px rgba(76, 56, 32, 0.12);
  }

  .code-block-wrapper :global(pre) {
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.12);
  }

  :global(html[data-theme="dark"]) .code-block-wrapper {
    border-color: rgba(45, 59, 75, 0.95);
    background:
      radial-gradient(circle at 95% 4%, rgba(86, 198, 194, 0.16), transparent 28%),
      linear-gradient(160deg, rgba(24, 35, 46, 0.96), rgba(17, 25, 35, 0.92));
    box-shadow:
      inset 0 0 0 1px rgba(86, 198, 194, 0.12),
      0 20px 46px rgba(0, 0, 0, 0.34);
  }

  :global(html[data-theme="dark"]) .code-block-wrapper :global(pre) {
    box-shadow: inset 0 0 0 1px rgba(86, 198, 194, 0.2);
  }
</style>
