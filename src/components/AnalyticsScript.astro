---
const token = import.meta.env.PUBLIC_CF_ANALYTICS_TOKEN;
const beaconConfig = token ? JSON.stringify({ token }) : "";
---

{
  token && (
    <script
      defer
      src="https://static.cloudflareinsights.com/beacon.min.js"
      data-cf-beacon={beaconConfig}
    ></script>
  )
}
